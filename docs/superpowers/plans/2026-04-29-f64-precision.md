# f32→f64 中间精度提升 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将计算管线中的中间浮点类型从 f32 全面切换为 f64，消除聚合精度偏差（当前最大 7e-6，目标 < 1e-6）

**Architecture:** BigWig 读取保持 f32（磁盘格式不可变），进入覆盖度缓冲区时立即转为 f64，f64 贯穿聚合、MatrixRow 存储、排序指标，直到 formatting 输出。BED score 字段保持 f32，调用 write_matrix_value 时 cast。

**Tech Stack:** Rust, f64 arithmetic

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `src/pipeline/core/worker.rs` | Modify | Coverage buffers, aggregate_slice, compute_row 全部 f32→f64 |
| `src/pipeline/core/traits.rs:68-75` | Modify | `postprocess_row` 签名 `Vec<f32>` → `Vec<f64>` |
| `src/pipeline/matrix.rs` | Modify | `MatrixRow.values` f32→f64, sort metrics f32→f64 |
| `src/pipeline/scale_regions.rs:110-137` | Modify | `postprocess_row` 签名跟随 |
| `src/pipeline/reference_point.rs:90-111` | Modify | `postprocess_row` 签名跟随 |
| `src/io/writers/formatting.rs` | Modify | `write_matrix_value`/`format_plain_value` f32→f64, 去掉 `as f64` cast |
| `src/pipeline/core/mod.rs:135-147` | Modify | 已有测试 f32→f64 + 新增精度测试 |
| `src/pipeline/core/collector.rs:130,153` | Modify | 测试中 `dummy_row` 的 `Vec<f32>` → `Vec<f64>` |

---

### Task 1: worker.rs — 覆盖度缓冲区和聚合函数 f32→f64

**Files:**
- Modify: `src/pipeline/core/worker.rs`

- [ ] **Step 1: 修改覆盖度缓冲区池为 f64**

```rust
// Line 13-34: 修改 COVERAGE_POOL 和相关函数

thread_local! {
    static COVERAGE_POOL: std::cell::RefCell<Vec<Vec<f64>>> =
        std::cell::RefCell::new(Vec::new());
}

fn take_coverage_buffers(sample_count: usize, window_len: usize, default_fill: f64) -> Vec<Vec<f64>> {
    COVERAGE_POOL.with(|pool| {
        let mut bufs = pool.borrow_mut();
        bufs.resize_with(sample_count, Vec::new);
        for buf in bufs.iter_mut() {
            buf.clear();
            buf.resize(window_len, default_fill);
        }
        std::mem::take(&mut *bufs)
    })
}

fn return_coverage_buffers(bufs: Vec<Vec<f64>>) {
    COVERAGE_POOL.with(|pool| {
        *pool.borrow_mut() = bufs;
    });
}
```

- [ ] **Step 2: 修改 aggregate_slice 为 f64**

```rust
// Line 53-143: 将 aggregate_slice 签名和内部全部改为 f64

pub(super) fn aggregate_slice(slice: &[f64], average_type: AverageTypeBins) -> Option<f64> {
    let len = slice.len();
    if len == 0 {
        return None;
    }

    match average_type {
        AverageTypeBins::Mean => {
            let mut sum = 0.0f64;
            let mut count = 0u32;
            for &value in slice {
                if !value.is_nan() {
                    sum += value;
                    count += 1;
                }
            }
            if count == 0 { None } else { Some(sum / count as f64) }
        }
        AverageTypeBins::Sum => {
            let mut sum = 0.0f64;
            let mut found = false;
            for &value in slice {
                if !value.is_nan() {
                    sum += value;
                    found = true;
                }
            }
            if found { Some(sum) } else { None }
        }
        AverageTypeBins::Min => {
            let mut min = f64::INFINITY;
            let mut found = false;
            for &value in slice {
                if !value.is_nan() {
                    min = min.min(value);
                    found = true;
                }
            }
            if found { Some(min) } else { None }
        }
        AverageTypeBins::Max => {
            let mut max = f64::NEG_INFINITY;
            let mut found = false;
            for &value in slice {
                if !value.is_nan() {
                    max = max.max(value);
                    found = true;
                }
            }
            if found { Some(max) } else { None }
        }
        AverageTypeBins::Std => {
            let mut sum = 0.0f64;
            let mut count = 0u32;
            for &value in slice {
                if !value.is_nan() {
                    sum += value;
                    count += 1;
                }
            }
            if count == 0 {
                return None;
            }
            let mean = sum / count as f64;
            let mut variance_sum = 0.0f64;
            for &value in slice {
                if !value.is_nan() {
                    let delta = value - mean;
                    variance_sum += delta * delta;
                }
            }
            Some((variance_sum / count as f64).sqrt())
        }
        AverageTypeBins::Median => {
            let mut values: Vec<f64> = slice
                .iter()
                .copied()
                .filter(|v| !v.is_nan())
                .collect();
            if values.is_empty() {
                return None;
            }
            values.sort_by(|a, b| a.partial_cmp(b).unwrap());
            let mid = values.len() / 2;
            if values.len() % 2 == 0 {
                Some((values[mid - 1] + values[mid]) / 2.0)
            } else {
                Some(values[mid])
            }
        }
    }
}
```

- [ ] **Step 3: 修改 should_skip_row_flat 为 f64**

```rust
// Line 146-183

fn should_skip_row_flat(values: &[f64], general: &GeneralOptions) -> bool {
    if general.skip_zeros {
        let mut all_zero = true;
        for &value in values {
            if value.is_nan() {
                continue;
            }
            if value != 0.0 {
                all_zero = false;
                break;
            }
        }
        if all_zero {
            return true;
        }
    }

    if let Some(min_threshold) = general.min_threshold {
        if values
            .iter()
            .filter(|value| !value.is_nan())
            .any(|value| *value <= min_threshold)
        {
            return true;
        }
    }

    if let Some(max_threshold) = general.max_threshold {
        if values
            .iter()
            .filter(|value| !value.is_nan())
            .any(|value| *value >= max_threshold)
        {
            return true;
        }
    }

    false
}
```

注意：`min_threshold` 和 `max_threshold` 本身就是 f64，所以去掉了 `*value as f64` cast。

- [ ] **Step 4: 修改 compute_sample_bins 为 f64**

Line 186-322 内具体变更（行号对应当前源码）:

```rust
// Line 192: 返回类型
) -> Result<Vec<f64>> {
```

```rust
// Line 198: NAN 类型
        return Ok(vec![f64::NAN; bin_count]);
```

```rust
// Line 204: NAN 类型
    return Ok(vec![f64::NAN; bin_count]);
```

```rust
// Line 208-212: default_fill 类型
let default_fill = if general.missing_data_as_zero {
    0.0f64
} else {
    f64::NAN
};
```

```rust
// Line 214-216: COVERAGE_BUF thread_local 类型
thread_local! {
    static COVERAGE_BUF: std::cell::RefCell<Vec<f64>> = std::cell::RefCell::new(Vec::new());
}
```

```rust
// Line 254: interval.value 填充 — f32 from disk → f64
buf[rel_start..rel_end].fill(f64::from(interval.value));
```

```rust
// Line 285: 同上，另一处 fill
buf[rel_start..rel_end].fill(f64::from(interval.value));
```

```rust
// Line 305: unwrap_or NAN
let mut value = value.unwrap_or(f64::NAN);
```

```rust
// Line 308-309: NAN
    value = f64::NAN;
```

```rust
// Line 312: scale_factor 不再需要 as f32 cast（scale_factor 本身就是 f64）
    value *= general.scale_factor;
```

```rust
// Line 318: 返回类型
Ok::<Vec<f64>, anyhow::Error>(bins)
```

- [ ] **Step 5: 修改 compute_row 返回类型为 f64**

```rust
// Line 324-344

pub fn compute_row<P: RegionPlan>(
    samples: &mut [Sample],
    record: &BedRecord,
    plan: &P,
    general: &GeneralOptions,
    nan_after_end: bool,
) -> Result<Option<(Vec<f64>, usize, usize)>> {
    let sample_count = samples.len();
    let bin_count = plan.bins().len();
    let mut all_values = Vec::with_capacity(sample_count * bin_count);
    for sample in samples.iter_mut() {
        let values = compute_sample_bins(sample, record, plan, general, nan_after_end)?;
        all_values.extend(values);
    }

    if should_skip_row_flat(&all_values, general) {
        return Ok(None);
    }

    Ok(Some((all_values, sample_count, bin_count)))
}
```

- [ ] **Step 6: 修改 process_batch 中的 f32 为 f64**

Line 355-512 内具体变更（行号对应当前源码）:

```rust
// Line 388-392: default_fill 类型
let default_fill = if general.missing_data_as_zero {
    0.0f64
} else {
    f64::NAN
};
```

```rust
// Line 436: interval.value 填充 — f32 from disk → f64
cov[rs as usize..re as usize].fill(f64::from(v.value));
```

```rust
// Line 482: unwrap_or NAN
let mut value = value.unwrap_or(f64::NAN);
```

```rust
// Line 485: NAN
    value = f64::NAN;
```

```rust
// Line 489: scale_factor 不再需要 as f32 cast
    value *= general.scale_factor;
```

- [ ] **Step 7: 确认编译（仅 worker.rs 内部，下游会报错，先不管）**

Run: `cargo check 2>&1 | head -30`
Expected: 类型不匹配的编译错误指向 traits.rs, matrix.rs 等下游文件

---

### Task 2: 接口层 — traits.rs 和 mode 实现

**Files:**
- Modify: `src/pipeline/core/traits.rs:68-75`
- Modify: `src/pipeline/scale_regions.rs:110-137`
- Modify: `src/pipeline/reference_point.rs:90-111`

- [ ] **Step 1: 修改 PipelineMode trait 中 postprocess_row 签名**

```rust
// src/pipeline/core/traits.rs, line 68-75

fn postprocess_row(
    &self,
    record: BedRecord,
    values: Vec<f64>,
    sample_count: usize,
    bin_count: usize,
    metadata: &Self::Metadata,
) -> MatrixRow;
```

- [ ] **Step 2: 修改 ScaleRegionsMode::postprocess_row**

```rust
// src/pipeline/scale_regions.rs, line 110-137

fn postprocess_row(
    &self,
    record: BedRecord,
    mut values: Vec<f64>,
    sample_count: usize,
    bin_count: usize,
    _metadata: &Self::Metadata,
) -> MatrixRow {
    if matches!(record.strand, Strand::Negative) {
        for sample_idx in 0..sample_count {
            let start = sample_idx * bin_count;
            values[start..start + bin_count].reverse();
        }
    }
    let exon_coords = if self.keep_exons {
        record.exons()
    } else {
        None
    };
    MatrixRow {
        record,
        values,
        sample_count,
        bin_count,
        exon_coords,
    }
}
```

- [ ] **Step 3: 修改 ReferencePointMode::postprocess_row**

```rust
// src/pipeline/reference_point.rs, line 90-111

fn postprocess_row(
    &self,
    record: BedRecord,
    values: Vec<f64>,
    sample_count: usize,
    bin_count: usize,
    _metadata: &Self::Metadata,
) -> MatrixRow {
    let exon_coords = if self.keep_exons {
        record.exons()
    } else {
        None
    };
    MatrixRow {
        record,
        values,
        sample_count,
        bin_count,
        exon_coords,
    }
}
```

---

### Task 3: MatrixRow 和排序 — matrix.rs

**Files:**
- Modify: `src/pipeline/matrix.rs`

- [ ] **Step 1: 修改 MatrixRow.values 类型**

```rust
// Line 199-215

pub struct MatrixRow {
    pub record: BedRecord,
    pub values: Vec<f64>,
    pub sample_count: usize,
    pub bin_count: usize,
    pub exon_coords: Option<Vec<(u32, u32)>>,
}

impl MatrixRow {
    pub fn flattened_values(&self) -> Vec<f64> {
        self.values.clone()
    }
}
```

- [ ] **Step 2: 修改排序相关函数为 f64**

```rust
// Line 302: metrics 类型
let metrics: Vec<f64> = self
    .rows
    .iter()
    .map(|row| compute_sort_metric(row, sort_using, sample_list))
    .collect();

// Line 397-451: compute_sort_metric 全部 f32→f64
fn compute_sort_metric(
    row: &MatrixRow,
    sort_using: SortUsing,
    sample_list: Option<&[usize]>,
) -> f64 {
    match sort_using {
        SortUsing::RegionLength => row.record.length() as f64,
        SortUsing::Sum => {
            let values = collect_values(row, sample_list);
            values.into_iter().fold(0.0f64, |acc, value| acc + value)
        }
        SortUsing::Mean => {
            let values = collect_values(row, sample_list);
            if values.is_empty() {
                f64::NAN
            } else {
                values.iter().copied().sum::<f64>() / values.len() as f64
            }
        }
        SortUsing::Median => {
            let mut values = collect_values(row, sample_list);
            if values.is_empty() {
                f64::NAN
            } else {
                values.sort_by(|a, b| a.partial_cmp(b).unwrap_or(Ordering::Equal));
                let mid = values.len() / 2;
                if values.len() % 2 == 1 {
                    values[mid]
                } else {
                    (values[mid - 1] + values[mid]) / 2.0
                }
            }
        }
        SortUsing::Max => {
            let values = collect_values(row, sample_list);
            if values.is_empty() {
                f64::NAN
            } else {
                values
                    .into_iter()
                    .fold(f64::NEG_INFINITY, |acc, value| acc.max(value))
            }
        }
        SortUsing::Min => {
            let values = collect_values(row, sample_list);
            if values.is_empty() {
                f64::NAN
            } else {
                values
                    .into_iter()
                    .fold(f64::INFINITY, |acc, value| acc.min(value))
            }
        }
    }
}

// Line 453-471: collect_values
fn collect_values(row: &MatrixRow, sample_list: Option<&[usize]>) -> Vec<f64> {
    let bin_count = row.bin_count;
    let mut values = Vec::new();
    match sample_list {
        Some(indices) => {
            for &sample_index in indices {
                let start = sample_index * bin_count;
                if start < row.values.len() {
                    let end = (start + bin_count).min(row.values.len());
                    values.extend(row.values[start..end].iter().copied().filter(|v| !v.is_nan()));
                }
            }
        }
        None => {
            values.extend(row.values.iter().copied().filter(|v| !v.is_nan()));
        }
    }
    values
}

// Line 473-480: compare_ascending
fn compare_ascending(left: f64, right: f64) -> Ordering {
    match (left.is_nan(), right.is_nan()) {
        (true, true) => Ordering::Equal,
        (true, false) => Ordering::Greater,
        (false, true) => Ordering::Less,
        (false, false) => left.partial_cmp(&right).unwrap_or_else(|| Ordering::Equal),
    }
}
```

- [ ] **Step 3: 确认 row_is_all_zero 无需显式改动**

```rust
// Line 516-526: 类型自动跟随 MatrixRow.values，0.0 字面量自动推导为 f64
fn row_is_all_zero(row: &MatrixRow) -> bool {
    for &value in &row.values {
        if value.is_nan() {
            continue;
        }
        if value != 0.0 {
            return false;
        }
    }
    true
}
```

---

### Task 4: 输出格式化 — formatting.rs

**Files:**
- Modify: `src/io/writers/formatting.rs`

- [ ] **Step 1: 修改 write_matrix_value 签名为 f64，去掉 as f64 cast**

```rust
// Line 91-116

pub fn write_matrix_value<W: Write>(writer: &mut W, value: f64) -> io::Result<()> {
    if value.is_nan() {
        return writer.write_all(b"nan");
    }

    if value.is_infinite() {
        return if value.is_sign_negative() {
            writer.write_all(b"-inf")
        } else {
            writer.write_all(b"inf")
        };
    }

    if value > -1e7 && value < 1e7 {
        let scaled = (value * 1_000_000.0).round_ties_even();
        return write_scaled_i64(writer, scaled as i64);
    }

    let scaled = (value * 1_000_000.0).round_ties_even();
    if !scaled.is_finite() || scaled.abs() > i128::MAX as f64 {
        let fallback = format!("{value:.6}");
        return writer.write_all(fallback.as_bytes());
    }

    write_scaled_i128(writer, scaled as i128)
}
```

注意：去掉了原来的 `value.is_finite() &&` 冗余检查（前面已经处理了 nan/inf）和所有 `value as f64` cast（参数已经是 f64）。

- [ ] **Step 2: 修改 format_plain_value 签名为 f64**

```rust
// Line 176-182

pub fn format_plain_value(value: f64) -> String {
    if value.is_nan() {
        "nan".to_string()
    } else {
        format!("{value:.4}")
    }
}
```

- [ ] **Step 3: 修改 write_matrix_row 中 BED score 调用处**

```rust
// Line 50-51: score 是 Option<f32>，需要 cast
} else if let Some(score) = row.record.score {
    write_matrix_value(&mut *buffer, f64::from(score))?;
```

- [ ] **Step 4: 修改测试为 f64**

```rust
// Line 188-248: 所有测试中 f32 → f64

fn fmt_value(value: f64) -> String {
    let mut buf = Vec::new();
    write_matrix_value(&mut buf, value).unwrap();
    String::from_utf8(buf).unwrap()
}

#[test]
fn nan_and_infinity() {
    assert_eq!(fmt_value(f64::NAN), "nan");
    assert_eq!(fmt_value(f64::INFINITY), "inf");
    assert_eq!(fmt_value(f64::NEG_INFINITY), "-inf");
}

#[test]
fn zero() {
    assert_eq!(fmt_value(0.0), "0.000000");
}

#[test]
fn small_positive() {
    assert_eq!(fmt_value(0.5), "0.500000");
    assert_eq!(fmt_value(1.0), "1.000000");
    assert_eq!(fmt_value(1.5), "1.500000");
    assert_eq!(fmt_value(0.001), "0.001000");
}

#[test]
fn small_negative() {
    assert_eq!(fmt_value(-0.5), "-0.500000");
    assert_eq!(fmt_value(-1.0), "-1.000000");
    assert_eq!(fmt_value(-1.5), "-1.500000");
}

#[test]
fn large_still_in_i64_range() {
    let v = 9_999_999.0f64;
    let result = fmt_value(v);
    assert!(result.starts_with("9999999"));
    assert!(result.ends_with("000000"));
}

#[test]
fn very_large_falls_back_to_i128() {
    let v = 1e8f64;
    let result = fmt_value(v);
    assert!(!result.contains("nan") && !result.contains("inf"));
}

#[test]
fn rounding_ties_to_even() {
    assert_eq!(fmt_value(0.0000005), "0.000000");
    assert_eq!(fmt_value(0.0000015), "0.000002");
}

#[test]
fn max_precision_values() {
    assert_eq!(fmt_value(0.123456), "0.123456");
    assert_eq!(fmt_value(0.000001), "0.000001");
    assert_eq!(fmt_value(0.999999), "0.999999");
}
```

---

### Task 5: 测试代码修复

**Files:**
- Modify: `src/pipeline/core/mod.rs:135-147`
- Modify: `src/pipeline/core/collector.rs:130,153`

- [ ] **Step 1: 修改 aggregate_slice 已有测试为 f64**

```rust
// src/pipeline/core/mod.rs, line 135-147

#[test]
fn aggregate_slice_ignores_nans() {
    let data = [1.0, f64::NAN, 3.0, 5.0];
    let mean = aggregate_slice(&data, AverageTypeBins::Mean).unwrap();
    assert!((mean - 3.0).abs() < 1e-6);

    let max = aggregate_slice(&data, AverageTypeBins::Max).unwrap();
    assert_eq!(max, 5.0);

    let median = aggregate_slice(&data, AverageTypeBins::Median).unwrap();
    assert_eq!(median, 3.0);
}
```

- [ ] **Step 2: 新增精度回归测试**

```rust
// src/pipeline/core/mod.rs, 在 aggregate_slice_ignores_nans 测试之后添加

#[test]
fn aggregate_mean_f64_precision() {
    // Values in the 15-42 range where f32 accumulation causes >5e-6 error.
    // 35.92 cannot be exactly represented in f32 (nearest: 35.919998).
    // Averaging 9 copies in f32 gives ~7e-6 drift; f64 keeps error < 1e-10.
    let val = 35.92f64;
    let data = vec![val; 9];
    let mean = aggregate_slice(&data, AverageTypeBins::Mean).unwrap();
    assert!(
        (mean - val).abs() < 1e-10,
        "f64 mean drift too large: expected {val}, got {mean}, delta {}",
        (mean - val).abs()
    );
}
```

- [ ] **Step 3: 修改 collector.rs 测试中的 dummy_row**

```rust
// src/pipeline/core/collector.rs, line 130

fn dummy_row(name: &str, values: Vec<f64>) -> MatrixRow {
    MatrixRow {
        record: BedRecord {
            chrom: Arc::from("chr1"),
            start: 100,
            end: 200,
            name: Some(name.to_string()),
            score: None,
            score_raw: None,
            strand: Strand::Unstranded,
            strand_raw: None,
            extra_fields: vec![],
        },
        values,
        sample_count: 1,
        bin_count: 3,
        exon_coords: None,
    }
}
```

---

### Task 6: 编译验证 + 全量测试

- [ ] **Step 1: 编译**

Run: `cargo build --release 2>&1`
Expected: 编译成功，无错误（可能有 warning）

- [ ] **Step 2: 运行单元测试**

Run: `cargo test 2>&1`
Expected: 所有 28+ 个测试通过（含新增的 `aggregate_mean_f64_precision`）

- [ ] **Step 3: 运行 python parity 测试**

Run: `pixi run python scripts/full_python_compatibility.py --no-cache 2>&1`
Expected: 10/10 通过，包括之前失败的 `gtf_input`

- [ ] **Step 4: Commit**

```bash
git add src/pipeline/core/worker.rs src/pipeline/core/traits.rs \
  src/pipeline/core/mod.rs src/pipeline/core/collector.rs \
  src/pipeline/matrix.rs src/pipeline/scale_regions.rs \
  src/pipeline/reference_point.rs src/io/writers/formatting.rs
git commit -m "refactor: use f64 for intermediate computation to fix precision drift

Switches coverage buffers, aggregation, MatrixRow storage, and sort
metrics from f32 to f64.  BigWig values (on-disk f32) are promoted to
f64 on read; formatting accepts f64 directly, removing the redundant
as-f64 cast.  Fixes gtf_input parity test (max delta 7e-6 → <1e-10)."
```

---

### Task 7: 性能基准测试 + 报告

**Files:**
- Create: `bench_reports/<timestamp>-f64-precision.md`（由脚本生成）

**前置条件:** Task 6 的 `cargo build --release` 已成功完成。

- [ ] **Step 1: 运行 profile_bench.sh 生成性能报告**

```bash
scripts/profile_bench.sh \
  f64-precision \
  "f32→f64 intermediate precision upgrade" \
  "aggregate_slice process_batch" \
  -- \
  target/release/compute_matrix_rs scale-regions \
  -b 10 -p 4 \
  --regionsFileName target/compute-matrix-datasets/encode_k562_atac/ENCFF333TAT.bed \
  --scoreFileName \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF019IPA.bigWig \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF093IIW.bigWig \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF656ZKM.bigWig \
  -o /tmp/bench_f64.mat.gz
```

Expected: 脚本输出 "Report written to bench_reports/YYYY-MM-DD-HHMMSS-f64-precision.md"

- [ ] **Step 2: 写入 Hotspot Analysis 对比基线**

读取生成的报告，填写 `## Agent TODO: Hotspot Analysis` 部分。对比基线报告 `bench_reports/2026-04-29-000444-pipeline-v2-final.md`（以下为关键基线指标）：

| Metric | Baseline (pipeline-v2-final) |
|---|---|
| Wall clock (time -v) | 11.10 s |
| Wall clock (perf stat) | 11.218 s |
| User time | 28.14 s |
| System time | 2.23 s |
| Max RSS | 557,260 KB |
| task-clock | 30,561 ms |
| Page faults (time -v) | 151,959 |
| Voluntary ctx switches | 286 |
| Heaptrack alloc calls | 9,590,908 |
| Peak heap | 451.45 MB |

分析需包含:
1. **对比表**（baseline vs f64-precision: 壁钟时间、RSS、task-clock、上下文切换）
2. **Top 5 CPU 热点**及是否为优化目标
3. **Top 3 分配热点**及是否可缩减
4. **结论**: PASS（回归 ≤5%）/ FAIL（回归 >5%）

**预期影响:** f32→f64 主要影响 `aggregate_slice` (3.30% self) 和 `process_batch` (9.79% self)。f64 运算在 x86-64 上开销与 f32 接近（均为 SSE/AVX 64-bit 单元），但 `Vec<f64>` 内存占用翻倍可能导致：
- RSS 上升 ~20-50 MB（MatrixRow values 翻倍: ~270K rows × 300 bins × 3 samples × 4 extra bytes）
- Page faults 可能小幅上升
- 壁钟时间预期变化 < 3%（计算热点 zlib 43% 不受影响）

- [ ] **Step 3: 正确性验证 — 比较输出 md5**

```bash
# 生成基线输出
target/release/compute_matrix_rs scale-regions \
  -b 10 -p 4 \
  --regionsFileName target/compute-matrix-datasets/encode_k562_atac/ENCFF333TAT.bed \
  --scoreFileName \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF019IPA.bigWig \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF093IIW.bigWig \
    target/compute-matrix-datasets/encode_k562_atac/ENCFF656ZKM.bigWig \
  -o /tmp/bench_f64_verify.mat.gz

# 解压并比较（f64 输出会因精度改变而 md5 不同，这是预期行为）
# 验证方式：确认行数一致 + 数值差异在精度范围内
zcat /tmp/bench_f64_verify.mat.gz | tail -n +2 | wc -l
# Expected: 与 baseline 行数一致（269800 行）

# 可选：抽样比较（如果有 baseline 输出文件）
```

注意：由于 f32→f64 精度提升，输出值的小数位可能微调（e.g. `0.123457` → `0.123456`），md5 不一致是预期行为。验证重点是行数一致、无 NaN 异常。

- [ ] **Step 4: Commit 性能报告**

```bash
git add bench_reports/*-f64-precision.md
git commit -m "docs: add f64 precision upgrade performance report"
```
